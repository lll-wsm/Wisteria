# Wisteria 开发文档

> 针对问题记录中的 4 个待处理项制定开发方案。
> 目标：在保持现有架构（Electron + Vite + Muya）不变的前提下，逐个修复。

## 一、现状分析

| 文件 | 作用 | 与本任务的关系 |
| --- | --- | --- |
| `main.js` | Electron 主进程：窗口、菜单、文件读写、IPC | 编码、历史、语言菜单都在这里改 |
| `preload.js` | contextBridge 暴露 `window.electronAPI` | 新增 IPC 需同步补充 |
| `index.html` | 渲染层结构，UI 文案硬编码为英文 | 语言支持需改造 |
| `src/renderer.js` | 渲染层逻辑（打开/保存文件、侧边栏、查找替换） | 编码提示、历史、语言切换 |
| `src/style.css` | 样式 | 问题 4 的修改点 |
| `package.json` | 依赖与打包配置 | 新增依赖 + 打包文件白名单 |

当前没有设置持久化机制（主题跟随系统），所有文件读写均按 UTF-8 硬编码。

## 二、问题 1：无法切换打开文件的编码

### 现状

- `main.js` 中 `open-file` / `open-file-with-path` 使用 `fs.readFileSync(filePath, 'utf8')` 读取；
- 所有保存操作均按 UTF-8 写入；
- 打开 GBK 等非 UTF-8 文件会乱码，且无任何切换入口。

### 方案

1. 新增依赖（纯 JS，可在主进程使用）：
   - `iconv-lite`：编码转换；
   - `jschardet`：编码检测。
2. 读取流程（主进程）：
   - 先检查 BOM（UTF-8 / UTF-16 LE / UTF-16 BE），有 BOM 直接用 BOM 对应编码；
   - 无 BOM 时用 `jschardet` 检测，检测失败回退 UTF-8；
   - 用 `iconv-lite` 解码为字符串，并在返回值中携带 `encoding` 字段。
3. 保存流程：记住当前文件的编码，保存时用 `iconv-lite` 按原编码写回；BOM 文件保留 BOM；新建/未知文件默认 UTF-8。
4. 切换入口：
   - 菜单新增「File > Reopen with Encoding」，列出常用编码（UTF-8、GBK/GB18030、Big5、UTF-16LE、Shift_JIS、ISO-8859-1）；
   - 若当前文档有未保存修改，先提示确认再重新读取；
   - 状态栏显示当前文件编码。
5. IPC 变更：
   - `open-file` / `open-file-with-path` 返回增加 `encoding`；
   - 新增 `reopen-with-encoding(filePath, encoding)`；
   - `save-file*` 系列在返回中带出实际写入编码。

### 涉及文件

`main.js`、`preload.js`、`src/renderer.js`、`index.html`（状态栏）、`package.json`

### 验收标准

- 打开 GBK 编码的 .md 文件不乱码，可正常编辑保存；
- 保存后仍为原编码（可用 `file` 命令或重新打开验证）；
- 菜单可随时切换编码并重新加载文件。

## 三、问题 2：添加文件打开历史

### 现状

- 文件通过菜单 Open 或侧边栏树打开，文件夹通过 Open Folder 打开；
- 无任何历史记录，重启后无法找回之前打开的文件/目录。

### 方案

1. 持久化：在 `app.getPath('userData')` 下新建 `history.json`，保存打开过的文件和目录（含时间戳），去重、上限 20 条、最近优先。
2. 记录时机（主进程内自动记录，渲染层无感）：
   - `open-file`、`open-file-with-path` 成功打开文件时记录文件；
   - `open-folder` 成功打开目录时记录目录；
   - `save-file-as` 另存为成功后记录新路径。
3. 展示：
   - 菜单新增「File > Open Recent」子菜单：文件与目录混排，点击文件直接打开，点击目录打开工作区；
   - 历史条目渲染前过滤已不存在的路径。
4. IPC 变更：
   - 菜单提供「Clear Recent」清空全部历史；
   - 打开文件/目录的历史记录由主进程自动写入。

### 涉及文件

`main.js`、`preload.js`、`src/renderer.js`

### 验收标准

- 打开文件/目录后，重启应用仍能在 Recent 中看到并一键打开；
- 历史按最近使用排序，重复打开不产生重复条目，最多 20 条；
- 已删除的文件不再出现在列表中。

## 四、问题 3：新增语言支持（英汉）

### 现状

- 所有界面文案硬编码为英文：主进程菜单（File/Edit/View…）、`index.html`（Workspace、Find、Replace、状态栏等）、`renderer.js`（弹窗、确认框、空状态、输入占位符）。

### 方案

1. 轻量 i18n 方案，不引入重框架：
   - 新建 `locales/zh-CN.json` 与 `locales/en-US.json` 两份词典，key 统一；
   - 主进程与渲染层共用同一份 JSON（主进程 `require`，渲染层通过 Vite 引入）。
2. 语言设置持久化：`userData/settings.json` 中保存 `language`，默认跟随系统语言（中文系统默认中文，否则英文）。
3. 切换入口：
   - 菜单新增「View > Language」子菜单（中文 / English）；
   - 切换后主进程重建菜单（提取 `createMenu` 为可重复调用），并通过 IPC 通知渲染层刷新文案。
4. 渲染层改造：
   - 静态文案改用 `data-i18n` 属性 + 启动时统一替换；
   - 动态文案（状态栏、空状态、占位符、alert/confirm）通过 `t(key)` 函数生成，切换语言时重新渲染；
   - 新增文案（编码菜单、历史区域）同步进词典。
5. 覆盖范围：菜单、侧边栏、查找替换面板、浮动菜单、右键菜单、状态栏、弹窗/确认框、HTML 标题。

### 涉及文件

新增 `locales/`，修改 `main.js`、`preload.js`、`index.html`、`src/renderer.js`、`src/style.css`（若有语言相关样式）

### 验收标准

- 菜单与界面可在中文 / 英文之间完整切换，无遗漏硬编码文案；
- 重启后保持上次选择的语言；
- 切换语言不丢失当前编辑内容。

## 五、问题 4：编辑区第一行与顶部间隔过大

### 现状（已定位根因）

`src/style.css` 中：

```css
#ag-editor-id {
  padding: 120px 10% !important;
}
```

顶部 120px 的 padding 导致第一行距离顶部过大。Muya 会设置内联样式，因此需要保留 `!important`。

### 方案

- 将顶部 padding 调小（如 `24px`），保留横向 `10%` 与底部 padding（滚动条安全区）；
- 建议初值：`padding: 24px 10% 120px !important;`，实际效果在 dev 模式肉眼确认后微调；
- 若改动后 Muya 内部仍有额外 margin，一并排查 `.muya-container` 与段落样式。

### 涉及文件

`src/style.css`

### 验收标准

- 新文档第一行与顶部间距明显缩小、观感正常；
- 水平方向与底部间距不受影响。

## 六、依赖与打包注意事项

1. 新增运行时依赖：`iconv-lite`、`jschardet`。
2. **打包配置必须同步调整**：当前 `package.json` 的 `build.files` 白名单包含 `!node_modules/**/*`（为瘦身排除全部依赖）。
   新增主进程依赖后，需在白名单中加入：

   ```json
   "node_modules/iconv-lite/**/*",
   "node_modules/jschardet/**/*"
   ```

   否则打包后的 app 会因找不到依赖而崩溃（asar 仍需保持瘦身，不能恢复为全量依赖）。
3. 打包后需重新跑 `./pack.sh --app` 验证依赖已包含且应用可启动。

## 七、实施顺序

1. **问题 4**（CSS 一处修改，风险最低，先做并肉眼验证）；
2. **问题 1 编码**（新增依赖 + 主进程读写改造 + 菜单/状态栏）；
3. **问题 2 打开历史**（持久化 + 菜单「Open Recent」）；
4. **问题 3 语言支持**（改造面最大，最后做；同时覆盖前两项新增的文案）；
5. 全量验证：`npm run dev` 手测 4 项功能 → `./pack.sh --app` 打包验证 → 更新 `问题记录.md` 状态 → 提交。

## 八、验证清单

- [ ] 编码：GBK 文件打开不乱码、保存后编码不变、可切换编码
- [ ] 历史：文件/目录可回溯、去重、上限 20、路径失效自动隐藏
- [ ] 语言：中英切换完整、设置持久化、菜单同步更新
- [ ] 间距：第一行距顶部明显缩小
- [ ] 打包：新依赖进入 asar，app 正常启动

## 九、UI 设计方案

### 1. 打开历史（仅菜单）

历史入口只放在菜单栏「File > Open Recent」，不在侧边栏展示。

交互规则：

- 点击文件条目：直接打开该文件（与树中点击一致，含自动保存切换逻辑）；
- 点击目录条目：将该目录作为工作区打开（等同 Open Folder）；
- 完整路径放在 `title` 提示中，文件名过长省略号截断；
- 渲染前过滤已不存在的路径；历史最多 20 条，去重。

### 2. 状态栏编码指示器

状态栏右侧新增编码指示（与字数统计同栏），无打开文件时隐藏：

```
┌──────────────────────────────────────────────┐
│ 128 words · 823 characters            GBK ▾ │
└──────────────────────────────────────────────┘
```

交互规则：

- 显示当前文件实际编码（检测结果或手动切换结果）；
- 点击弹出编码选择菜单（与 File 菜单中的 Reopen with Encoding 同一份列表）；
- 有未保存修改时，切换前弹原生确认框。

### 3. 菜单结构（File / View）

```
File                              View
  New File            ⌘N           Toggle Sidebar   ⌘B
  Open...             ⌘O           Toggle DevTools
  Open Recent        ▸             ─────────────
    ──────────────                 Language        ▸
    笔记.md                          中文
    docs                             English        ✓
    ──────────────
    Clear Recent
  Reopen with Encoding ▸
    UTF-8             ✓
    GBK
    Big5
    UTF-16 LE
    Shift_JIS
  ──────────────
  Save                ⌘S
  ...
```

交互规则：

- 当前文件的编码前打勾；
- 历史数据由主进程持久化维护，菜单每次构建时读取；
- Language 切换即时生效：主进程重建菜单 + 通知渲染层刷新文案，无需重启；
- 未打开文件时，Reopen with Encoding 置灰。

### 4. 编辑区顶部间距

纯样式调整，无新增 UI。`#ag-editor-id` 顶部 padding 由 `120px` 调为 `24px`，横向 `10%`、底部 `120px` 不变，肉眼确认后微调。
