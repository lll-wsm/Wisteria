# 问题记录

> 记录使用 Wisteria 过程中遇到的问题。
> 当前阶段：只记录问题，暂不优化。

## 问题 1：无法切换打开文件的编码

- 状态：已修复
- 日期：2026-08-27
- 描述：打开文件时无法切换/选择文件编码。遇到非 UTF-8 编码的文件（如 GBK）时，无法正常读取或显示内容。
- 修复：新增 iconv-lite + jschardet 自动检测编码；保存按原编码写回（保留 BOM）；菜单「以其他编码重新打开」+ 状态栏编码指示器可随时切换。

## 问题 2：添加文件打开历史

- 状态：已修复
- 日期：2026-08-27
- 类型：功能需求
- 描述：希望添加文件打开历史功能，方便查看之前打开过的文件及目录。
- 修复：历史持久化到 userData/history.json（去重、上限 20）；菜单「文件 > 打开最近」可一键打开文件/目录，「清除最近记录」清空。

## 问题 3：新增语言支持（英汉）

- 状态：已修复
- 日期：2026-08-28
- 类型：功能需求
- 描述：希望新增界面语言支持（英文 / 中文），可切换显示语言。
- 修复：新增 locales 词典（zh-CN / en-US），默认跟随系统语言；菜单「视图 > 语言」即时切换，主进程菜单与渲染层界面同步更新，设置持久化。

## 问题 4：编辑区第一行与顶部间隔过大

- 状态：已修复
- 日期：2026-08-28
- 描述：编辑区第一行距离顶部（头部区域）间隔过大，观感不紧凑。
- 修复：`#ag-editor-id` 顶部 padding 由 120px 调整为 24px。

## 问题 5：代码块输入 `(` / `()` 时内容"被清空"

- 状态：已修复
- 日期：2026-08-31
- 描述：在 Mermaid 代码块（或其他代码块）内输入 `(` / `()`，图表预览瞬间消失，观感上像"数据被清空"。用户此前遇到过并修复过，近期修改后又出现。
- 排查：
  - `问题记录.md` / `开发文档.md` 中无此问题分析；git 历史中 `tauri-migration` 分支有"修复代码块"系列提交（2026-06-10），属于另一条开发线，Electron 主线从未合入。
  - 用真实键盘事件流（keyDown→char→keyUp）复现：无输入法直接输入时，编辑区文本/markdown 数据**从未丢失**（段落、JS 代码块、Mermaid 源码、快速 `()()()` 连打均完整）。
  - **真正的根因**：开着中文输入法（或其他 IME）在 Mermaid 代码块源码中输入 `(` 时，浏览器进入 composition（`isComposed=true`，input 事件被跳过、model 不同步）；此前新增的 MutationObserver 检测到源码 DOM 变化后在 250ms 后调用 `render(true)` **全量重建 DOM**，把合成中的字符（`(`）从 DOM 清掉，且打断输入法合成——合成结束时无字符可提交，输入永久丢失（表现：`(` 无法单独输入、代码块内容被清空）。该 observer 只匹配 `figure` 内的 Mermaid 源码，所以普通代码块无此问题。
- 修复：
  - `muya-core/src/muya/lib/parser/render/index.js`：`renderMermaid` catch 分支改为保留最后一次成功渲染的 svg（置 `ag-math-error` 类），仅当从未渲染成功时才显示错误占位；语法恢复后自动重新渲染恢复图表。
  - `src/style.css`：`ag-math-error` 状态下预览 svg 降透明度（0.35）提示当前语法错误，而不是把图表清掉。
  - `src/renderer.js`：**移除** 输入法合成期间触发全量重渲染的 `MutationObserver`（其原始目的——删除后预览不恢复——已由 renderMermaid 保留旧图修复覆盖，删除路径无需额外刷新）。
- 验证：构建产物与 `npm run dev` 两种模式下，用模拟 IME composition 事件验证：输入法合成中输入 `(` 后停顿 400ms（原触发观察器的时序），`(` 完整保留、合成正常提交；无输入法快速连打文本完整；删除使语法断裂时图表保留并置灰、无 "Invalid Mermaid Codes" 卡死。
